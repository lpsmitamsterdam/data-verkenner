import fixtureSingle from './gebiedsgerichtwerken.json'
import fixtureList from './gebiedsgerichtwerken-list.json'
import { Single, List } from './types'

export const singleFixture = fixtureSingle as Single
export const listFixture = fixtureList as List
export const fixtureId = 'DX20'
export const path = 'gebieden/gebiedsgerichtwerken/'

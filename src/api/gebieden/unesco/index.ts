import fixtureSingle from './unesco.json'
import fixtureList from './unesco-list.json'
import type { Single, List } from './types'

export const singleFixture = fixtureSingle as Single
export const listFixture = fixtureList as List
export const fixtureId = 'bufferzone'
export const path = 'gebieden/unesco/'

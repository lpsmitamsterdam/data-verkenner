import fixtureSingle from './situeringen.json'
import fixtureList from './situeringen-list.json'
import type { Single, List } from './types'

export const singleFixture = fixtureSingle as Single
export const listFixture = fixtureList as List
export const path = 'monumenten/situeringen/'
export const fixtureId = '1'
